common:
  filter: TOOLCHAIN_HAS_NEWLIB == 1
sample:
  name: RTC module
tests:
  sample.basic.maxim_ds3231:
    build_only: true
    platform_allow:      
      - nucleo_f411re     
    integration_platforms:
      - efr32mg_sltb004a
